<h1><%= "Possible routes from #{@start_location} to #{@end_location}:" %> </h1>

<% @completedRoutes.each do |completedRoute| %>
  <h2>Possible Route:</h2> 
  <% completedRoute.each do |post| %>
    <%= render partial: "shared/post_display", locals: {post: post} %>
  <% end %>
<% end %>

    